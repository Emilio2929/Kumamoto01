services:
  - type: web
    name: kumamoto-backend
    runtime: docker
    rootDirectory: Backend
    dockerfilePath: Backend/Dockerfile
    envVars:
      - key: ASPNETCORE_ENVIRONMENT
        value: Production
